# flow: seek login code

Finish signing in by entering the one-time verification code SEEK emailed.
When a step refers to {{code}}, use that exact placeholder token as the value.

## enter the verification code

Type {{code}} into the verification / one-time-code field. If the code is split
across several single-digit boxes, type it into the first box (the rest usually
auto-advance).

## submit the code

Click the button that verifies and continues — labelled "Verify", "Continue",
"Confirm", or "Sign in". If submission happened automatically after the last
digit, skip this step.

## finished

The code has been submitted. Nothing more for the agent to do.
