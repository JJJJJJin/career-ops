// render-tracker — regenerate data/applications.md from the DB. Re-export of
// the shared helper so this tool can be called like any other.
export { renderTracker as renderTrackerString, writeTracker } from '../../shared/db/view.js';
